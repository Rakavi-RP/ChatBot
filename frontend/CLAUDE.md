# Frontend CLAUDE.md

Start every frontend-related response with "🎨 Frontend CLAUDE.md loaded".

## Scope

This directory contains the static chat UI (`index.html`, `style.css`, `app.js`) served by `../backend/server.py`. See the repo-root `CLAUDE.md` for full architecture details.
