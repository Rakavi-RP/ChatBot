# Backend CLAUDE.md

Start every backend-related response with "⚙️ Backend CLAUDE.md loaded".

## Scope

This directory contains `server.py`, the Python stdlib HTTP server that proxies chat requests to the Gemini API and serves static files from `../frontend/`. See the repo-root `CLAUDE.md` for full architecture details.
